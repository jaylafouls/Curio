// Empty stub for the `server-only` package under vitest. The real package throws
// when imported outside an RSC/bundler context; in unit tests we only exercise
// pure helpers from server modules, so an empty module is the correct stand-in.
export {}
